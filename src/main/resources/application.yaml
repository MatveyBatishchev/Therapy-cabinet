spring:
  application:
    name: therapy-cabinet
  datasource:
    driver-class-name: org.postgresql.Driver
  profiles:
    active: local
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
mybatis:
  config-location: classpath:mybatis/conf.xml
server:
  servlet:
    context-path: /therapy-cabinet
  port: 8080
springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /swagger.json
