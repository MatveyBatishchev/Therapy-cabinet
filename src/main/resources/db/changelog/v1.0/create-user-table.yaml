databaseChangeLog:
  - changeSet:
      id: create-customer-table
      author: VKamalov
      comment: "Создание таблицы с данными пользователей"
      changes:
        - createTable:
            tableName: customer
            remarks: "Таблица с данными пользователей"
            columns:
              - column:
                  name: id
                  type: serial
                  autoIncrement: true
                  remarks: "UUID пользователя (первичный ключ)"
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: login
                  type: varchar(127)
                  remarks: "Логин пользователя"
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: password
                  type: varchar(127)
                  remarks: "Пароль для входа (ключ шифрования BCrypt)"
                  constraints:
                    nullable: false
              - column:
                  name: roles
                  type: smallint
                  remarks: "Роли пользователя"
                  constraints:
                    nullable: false